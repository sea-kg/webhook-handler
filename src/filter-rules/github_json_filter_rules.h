#ifndef GITHUB_JSON_FILTER_H
#define GITHUB_JSON_FILTER_H

class GithubJsonFilter {
    public:
        GithubJsonFilter();
    private:
        std::string TAG;
        std::string m_sId;
};

#endif // GITHUB_JSON_FILTER_H